# MIN OVER

一个纯前端的组合搜索工具：在给定数字中寻找总和严格大于目标值、且总和最小的组合。

## 本地运行

直接打开 `index.html` 即可使用，无需安装依赖或启动服务器。

## GitHub Pages

项目通过 GitHub Actions 自动部署到 Pages。推送到 `main` 分支后，访问：

`https://disagree1105.github.io/order-min-submit/`
